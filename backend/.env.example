# Copy this file to .env and fill in your own key to enable the real LLM assistant.
# Without this, /ai/query automatically falls back to the rule-based assistant.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
